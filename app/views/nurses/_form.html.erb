<%= form_with(model: nurse) do |form| %>
  <% if nurse.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(nurse.errors.count, "error") %> prohibited this nurse from being saved:</h2>

      <ul>
        <% nurse.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :department, style: "display: block" %>
    <%= form.text_field :department %>
  </div>

  <div>
    <%= form.label :contact_number, style: "display: block" %>
    <%= form.number_field :contact_number %>
  </div>
  <div>
  <%= form.label :hospital_id, style: "display: block" %>
  <%= form.collection_select :hospital_id, Hospital.all, :id, :name, {}, { class: 'form-control' } %>
</div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
